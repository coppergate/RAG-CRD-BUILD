// Code generated by ent, DO NOT EDIT.

package ent

import (
	"app-builds/common/ent/inferencenode"
	"app-builds/common/ent/modeldefinition"
	"app-builds/common/ent/modelexecutionmetric"
	"app-builds/common/ent/predicate"
	"app-builds/common/ent/session"
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ModelExecutionMetricQuery is the builder for querying ModelExecutionMetric entities.
type ModelExecutionMetricQuery struct {
	config
	ctx         *QueryContext
	order       []modelexecutionmetric.OrderOption
	inters      []Interceptor
	predicates  []predicate.ModelExecutionMetric
	withSession *SessionQuery
	withNode    *InferenceNodeQuery
	withModel   *ModelDefinitionQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ModelExecutionMetricQuery builder.
func (_q *ModelExecutionMetricQuery) Where(ps ...predicate.ModelExecutionMetric) *ModelExecutionMetricQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *ModelExecutionMetricQuery) Limit(limit int) *ModelExecutionMetricQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *ModelExecutionMetricQuery) Offset(offset int) *ModelExecutionMetricQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *ModelExecutionMetricQuery) Unique(unique bool) *ModelExecutionMetricQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *ModelExecutionMetricQuery) Order(o ...modelexecutionmetric.OrderOption) *ModelExecutionMetricQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QuerySession chains the current query on the "session" edge.
func (_q *ModelExecutionMetricQuery) QuerySession() *SessionQuery {
	query := (&SessionClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(modelexecutionmetric.Table, modelexecutionmetric.FieldID, selector),
			sqlgraph.To(session.Table, session.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, modelexecutionmetric.SessionTable, modelexecutionmetric.SessionColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryNode chains the current query on the "node" edge.
func (_q *ModelExecutionMetricQuery) QueryNode() *InferenceNodeQuery {
	query := (&InferenceNodeClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(modelexecutionmetric.Table, modelexecutionmetric.FieldID, selector),
			sqlgraph.To(inferencenode.Table, inferencenode.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, modelexecutionmetric.NodeTable, modelexecutionmetric.NodeColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryModel chains the current query on the "model" edge.
func (_q *ModelExecutionMetricQuery) QueryModel() *ModelDefinitionQuery {
	query := (&ModelDefinitionClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(modelexecutionmetric.Table, modelexecutionmetric.FieldID, selector),
			sqlgraph.To(modeldefinition.Table, modeldefinition.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, modelexecutionmetric.ModelTable, modelexecutionmetric.ModelColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ModelExecutionMetric entity from the query.
// Returns a *NotFoundError when no ModelExecutionMetric was found.
func (_q *ModelExecutionMetricQuery) First(ctx context.Context) (*ModelExecutionMetric, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{modelexecutionmetric.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *ModelExecutionMetricQuery) FirstX(ctx context.Context) *ModelExecutionMetric {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ModelExecutionMetric ID from the query.
// Returns a *NotFoundError when no ModelExecutionMetric ID was found.
func (_q *ModelExecutionMetricQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{modelexecutionmetric.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *ModelExecutionMetricQuery) FirstIDX(ctx context.Context) int64 {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ModelExecutionMetric entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ModelExecutionMetric entity is found.
// Returns a *NotFoundError when no ModelExecutionMetric entities are found.
func (_q *ModelExecutionMetricQuery) Only(ctx context.Context) (*ModelExecutionMetric, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{modelexecutionmetric.Label}
	default:
		return nil, &NotSingularError{modelexecutionmetric.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *ModelExecutionMetricQuery) OnlyX(ctx context.Context) *ModelExecutionMetric {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ModelExecutionMetric ID in the query.
// Returns a *NotSingularError when more than one ModelExecutionMetric ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *ModelExecutionMetricQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{modelexecutionmetric.Label}
	default:
		err = &NotSingularError{modelexecutionmetric.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *ModelExecutionMetricQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ModelExecutionMetrics.
func (_q *ModelExecutionMetricQuery) All(ctx context.Context) ([]*ModelExecutionMetric, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ModelExecutionMetric, *ModelExecutionMetricQuery]()
	return withInterceptors[[]*ModelExecutionMetric](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *ModelExecutionMetricQuery) AllX(ctx context.Context) []*ModelExecutionMetric {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ModelExecutionMetric IDs.
func (_q *ModelExecutionMetricQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(modelexecutionmetric.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *ModelExecutionMetricQuery) IDsX(ctx context.Context) []int64 {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *ModelExecutionMetricQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*ModelExecutionMetricQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *ModelExecutionMetricQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *ModelExecutionMetricQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *ModelExecutionMetricQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ModelExecutionMetricQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *ModelExecutionMetricQuery) Clone() *ModelExecutionMetricQuery {
	if _q == nil {
		return nil
	}
	return &ModelExecutionMetricQuery{
		config:      _q.config,
		ctx:         _q.ctx.Clone(),
		order:       append([]modelexecutionmetric.OrderOption{}, _q.order...),
		inters:      append([]Interceptor{}, _q.inters...),
		predicates:  append([]predicate.ModelExecutionMetric{}, _q.predicates...),
		withSession: _q.withSession.Clone(),
		withNode:    _q.withNode.Clone(),
		withModel:   _q.withModel.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithSession tells the query-builder to eager-load the nodes that are connected to
// the "session" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ModelExecutionMetricQuery) WithSession(opts ...func(*SessionQuery)) *ModelExecutionMetricQuery {
	query := (&SessionClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withSession = query
	return _q
}

// WithNode tells the query-builder to eager-load the nodes that are connected to
// the "node" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ModelExecutionMetricQuery) WithNode(opts ...func(*InferenceNodeQuery)) *ModelExecutionMetricQuery {
	query := (&InferenceNodeClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withNode = query
	return _q
}

// WithModel tells the query-builder to eager-load the nodes that are connected to
// the "model" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ModelExecutionMetricQuery) WithModel(opts ...func(*ModelDefinitionQuery)) *ModelExecutionMetricQuery {
	query := (&ModelDefinitionClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withModel = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ResponseID int64 `json:"response_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ModelExecutionMetric.Query().
//		GroupBy(modelexecutionmetric.FieldResponseID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *ModelExecutionMetricQuery) GroupBy(field string, fields ...string) *ModelExecutionMetricGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ModelExecutionMetricGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = modelexecutionmetric.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ResponseID int64 `json:"response_id,omitempty"`
//	}
//
//	client.ModelExecutionMetric.Query().
//		Select(modelexecutionmetric.FieldResponseID).
//		Scan(ctx, &v)
func (_q *ModelExecutionMetricQuery) Select(fields ...string) *ModelExecutionMetricSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &ModelExecutionMetricSelect{ModelExecutionMetricQuery: _q}
	sbuild.label = modelexecutionmetric.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ModelExecutionMetricSelect configured with the given aggregations.
func (_q *ModelExecutionMetricQuery) Aggregate(fns ...AggregateFunc) *ModelExecutionMetricSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *ModelExecutionMetricQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !modelexecutionmetric.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *ModelExecutionMetricQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ModelExecutionMetric, error) {
	var (
		nodes       = []*ModelExecutionMetric{}
		_spec       = _q.querySpec()
		loadedTypes = [3]bool{
			_q.withSession != nil,
			_q.withNode != nil,
			_q.withModel != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ModelExecutionMetric).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ModelExecutionMetric{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withSession; query != nil {
		if err := _q.loadSession(ctx, query, nodes, nil,
			func(n *ModelExecutionMetric, e *Session) { n.Edges.Session = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withNode; query != nil {
		if err := _q.loadNode(ctx, query, nodes, nil,
			func(n *ModelExecutionMetric, e *InferenceNode) { n.Edges.Node = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withModel; query != nil {
		if err := _q.loadModel(ctx, query, nodes, nil,
			func(n *ModelExecutionMetric, e *ModelDefinition) { n.Edges.Model = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *ModelExecutionMetricQuery) loadSession(ctx context.Context, query *SessionQuery, nodes []*ModelExecutionMetric, init func(*ModelExecutionMetric), assign func(*ModelExecutionMetric, *Session)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*ModelExecutionMetric)
	for i := range nodes {
		fk := nodes[i].SessionID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(session.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "session_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *ModelExecutionMetricQuery) loadNode(ctx context.Context, query *InferenceNodeQuery, nodes []*ModelExecutionMetric, init func(*ModelExecutionMetric), assign func(*ModelExecutionMetric, *InferenceNode)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*ModelExecutionMetric)
	for i := range nodes {
		fk := nodes[i].NodeID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(inferencenode.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "node_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *ModelExecutionMetricQuery) loadModel(ctx context.Context, query *ModelDefinitionQuery, nodes []*ModelExecutionMetric, init func(*ModelExecutionMetric), assign func(*ModelExecutionMetric, *ModelDefinition)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*ModelExecutionMetric)
	for i := range nodes {
		fk := nodes[i].ModelID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(modeldefinition.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "model_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (_q *ModelExecutionMetricQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *ModelExecutionMetricQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(modelexecutionmetric.Table, modelexecutionmetric.Columns, sqlgraph.NewFieldSpec(modelexecutionmetric.FieldID, field.TypeInt64))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, modelexecutionmetric.FieldID)
		for i := range fields {
			if fields[i] != modelexecutionmetric.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withSession != nil {
			_spec.Node.AddColumnOnce(modelexecutionmetric.FieldSessionID)
		}
		if _q.withNode != nil {
			_spec.Node.AddColumnOnce(modelexecutionmetric.FieldNodeID)
		}
		if _q.withModel != nil {
			_spec.Node.AddColumnOnce(modelexecutionmetric.FieldModelID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *ModelExecutionMetricQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(modelexecutionmetric.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = modelexecutionmetric.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ModelExecutionMetricGroupBy is the group-by builder for ModelExecutionMetric entities.
type ModelExecutionMetricGroupBy struct {
	selector
	build *ModelExecutionMetricQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *ModelExecutionMetricGroupBy) Aggregate(fns ...AggregateFunc) *ModelExecutionMetricGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *ModelExecutionMetricGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ModelExecutionMetricQuery, *ModelExecutionMetricGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *ModelExecutionMetricGroupBy) sqlScan(ctx context.Context, root *ModelExecutionMetricQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ModelExecutionMetricSelect is the builder for selecting fields of ModelExecutionMetric entities.
type ModelExecutionMetricSelect struct {
	*ModelExecutionMetricQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *ModelExecutionMetricSelect) Aggregate(fns ...AggregateFunc) *ModelExecutionMetricSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *ModelExecutionMetricSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ModelExecutionMetricQuery, *ModelExecutionMetricSelect](ctx, _s.ModelExecutionMetricQuery, _s, _s.inters, v)
}

func (_s *ModelExecutionMetricSelect) sqlScan(ctx context.Context, root *ModelExecutionMetricQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
