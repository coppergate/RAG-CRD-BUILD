// Code generated by ent, DO NOT EDIT.

package ent

import (
	"app-builds/common/ent/codeingestion"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
)

// CodeIngestion is the model entity for the CodeIngestion schema.
type CodeIngestion struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// S3BucketID holds the value of the "s3_bucket_id" field.
	S3BucketID string `json:"s3_bucket_id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the CodeIngestionQuery when eager-loading is set.
	Edges        CodeIngestionEdges `json:"edges"`
	selectValues sql.SelectValues
}

// CodeIngestionEdges holds the relations/edges for other nodes in the graph.
type CodeIngestionEdges struct {
	// Embeddings holds the value of the embeddings edge.
	Embeddings []*CodeEmbedding `json:"embeddings,omitempty"`
	// Tags holds the value of the tags edge.
	Tags []*Tag `json:"tags,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// EmbeddingsOrErr returns the Embeddings value or an error if the edge
// was not loaded in eager-loading.
func (e CodeIngestionEdges) EmbeddingsOrErr() ([]*CodeEmbedding, error) {
	if e.loadedTypes[0] {
		return e.Embeddings, nil
	}
	return nil, &NotLoadedError{edge: "embeddings"}
}

// TagsOrErr returns the Tags value or an error if the edge
// was not loaded in eager-loading.
func (e CodeIngestionEdges) TagsOrErr() ([]*Tag, error) {
	if e.loadedTypes[1] {
		return e.Tags, nil
	}
	return nil, &NotLoadedError{edge: "tags"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*CodeIngestion) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case codeingestion.FieldS3BucketID:
			values[i] = new(sql.NullString)
		case codeingestion.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case codeingestion.FieldID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the CodeIngestion fields.
func (_m *CodeIngestion) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case codeingestion.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case codeingestion.FieldS3BucketID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field s3_bucket_id", values[i])
			} else if value.Valid {
				_m.S3BucketID = value.String
			}
		case codeingestion.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the CodeIngestion.
// This includes values selected through modifiers, order, etc.
func (_m *CodeIngestion) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryEmbeddings queries the "embeddings" edge of the CodeIngestion entity.
func (_m *CodeIngestion) QueryEmbeddings() *CodeEmbeddingQuery {
	return NewCodeIngestionClient(_m.config).QueryEmbeddings(_m)
}

// QueryTags queries the "tags" edge of the CodeIngestion entity.
func (_m *CodeIngestion) QueryTags() *TagQuery {
	return NewCodeIngestionClient(_m.config).QueryTags(_m)
}

// Update returns a builder for updating this CodeIngestion.
// Note that you need to call CodeIngestion.Unwrap() before calling this method if this CodeIngestion
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *CodeIngestion) Update() *CodeIngestionUpdateOne {
	return NewCodeIngestionClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the CodeIngestion entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *CodeIngestion) Unwrap() *CodeIngestion {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: CodeIngestion is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *CodeIngestion) String() string {
	var builder strings.Builder
	builder.WriteString("CodeIngestion(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("s3_bucket_id=")
	builder.WriteString(_m.S3BucketID)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// CodeIngestions is a parsable slice of CodeIngestion.
type CodeIngestions []*CodeIngestion
