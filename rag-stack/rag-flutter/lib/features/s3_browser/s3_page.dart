import 'package:flutter/material.dart';

class S3Page extends StatelessWidget {
  const S3Page({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('S3 Object Browser')),
      body: const Center(child: Text('S3 Object Browser (Phase 2)')),
    );
  }
}
