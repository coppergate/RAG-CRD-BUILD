apiVersion: v1
kind: Namespace
metadata:
  name: container-registry
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: registry-pvc
  namespace: container-registry
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  storageClassName: rook-ceph-block
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: registry
  namespace: container-registry
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: registry
  template:
    metadata:
      labels:
        app: registry
    spec:
      nodeSelector:
        role: storage-node
      containers:
      - name: registry
        image: 10.0.0.1:5000/registry:2
        ports:
        - containerPort: 5000
        env:
        - name: REGISTRY_HTTP_TLS_CERTIFICATE
          value: /certs/tls.crt
        - name: REGISTRY_HTTP_TLS_KEY
          value: /certs/tls.key
        volumeMounts:
        - name: storage
          mountPath: /var/lib/registry
        - name: certs
          mountPath: /certs
          readOnly: true
      volumes:
      - name: storage
        persistentVolumeClaim:
          claimName: registry-pvc
      - name: certs
        secret:
          secretName: in-cluster-registry-tls
---
apiVersion: v1
kind: Service
metadata:
  name: registry
  namespace: container-registry
  annotations:
    purelb.io/addresspool: default
spec:
  type: LoadBalancer
  loadBalancerIP: 172.20.1.26
  ports:
  - port: 5000
    targetPort: 5000
  selector:
    app: registry
